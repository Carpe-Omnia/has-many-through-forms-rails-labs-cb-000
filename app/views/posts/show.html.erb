
<%= @post.title %>
<%= @post.content %>

<% form_for @comment do |f| %>
  <%= f.text_field :content %>
  <%= f.collection_select :users %>
<% end %>

<% @post.comments.each do |c| %>
  <%= c.content %>
  <%= c.user.name %>
<% end %>
